import styled from "styled-components";

export const Header = styled.div`
  text-align: center;
  padding: 2rem;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 0.5rem;
  .icon {
    color: var(--yellow);
  }
`;
